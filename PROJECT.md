# Souped Up 2026 — เอกสารโปรเจกต์ / Project Overview

ระบบสมัครสมาชิกและลงทะเบียนการแข่งขันรถยนต์ **Souped Up 2026** — เว็บแอปหน้าเดียว (single-file) โทนสีเข้มแนวแข่งรถ รองรับ 2 ภาษา (ไทย/อังกฤษ, ค่าเริ่มต้น: ไทย) ทำงานได้ทันทีในเบราว์เซอร์ ไม่ต้อง build ไม่มี dependency

- **Live (GitHub Pages):** https://nidss.github.io/su/
- **Artifact (พรีวิว):** https://claude.ai/code/artifact/f2b8159f-ea2f-46ca-926d-cfa8140f2ae0
- **สี:** มิ้นต์ `#6FFFE8` · เขียวมะนาว `#D1FF03` · กรมท่า `#0C142A`

> เป็น **front-end demo**: ระบบสมาชิก / การชำระเงิน / ข้อมูลใน dashboard เป็น mock ทั้งหมด ไม่มี backend จริง

---

## เทคโนโลยี / Tech

- **HTML/CSS/JS ล้วน** ในไฟล์เดียว (`index.html`) — ไม่มี framework, ไม่มีขั้นตอน build
- ภาพ/โลโก้/ฟอนต์สำรอง ฝังเป็น **base64 / SVG data URI** ในไฟล์ ทำให้เปิดที่ไหนก็แสดงครบ
- ไอคอน: Lucide (ฝัง SVG ไว้ในโค้ด)
- ฟอนต์: Rajdhani + Noto Sans Thai (โหลดจาก Google Fonts, มี fallback ระบบ)

**การรัน:** เปิด `index.html` ในเบราว์เซอร์ได้เลย หรือใช้งานผ่าน GitHub Pages

---

## ขั้นตอนการลงทะเบียน (9 ขั้น) / Registration Flow

| # | ขั้นตอน | รายละเอียด |
|---|---------|-----------|
| 1 | **สมาชิก** | สมัครสมาชิก / เข้าสู่ระบบ (แบบ Tab). เข้าสู่ระบบใช้ข้อมูลอะไรก็ได้ (demo) → เข้าหน้า "ประวัติของฉัน" |
| 2 | **ข้อตกลงและเงื่อนไข** | อ่าน & ยอมรับเอกสาร 3 ฉบับ (นโยบายข้อมูล, กติกา, การค้ำประกันการแข่งขัน+ความเสียหาย) ผ่าน popup |
| 3 | **ข้อมูลผู้สมัคร** | วันเกิด (คลิกเปิด date picker), ที่อยู่ตามบัตร/จัดส่ง, ช่องทางติดต่อ (Line/WeChat/WhatsApp), อัปโหลดบัตรประชาชน |
| 4 | **ข้อมูลนักแข่ง** | รองรับ **หลายนักแข่ง** และ **หลายรุ่นการแข่งขันต่อคน** (ดูรายละเอียดด้านล่าง) |
| 5 | **ที่จอดรถ (พีท)** | เลือกช่องพีทจาก**ผังงานจริง** (39 เต็นท์ × 4 ช่อง = 152 ช่อง) — จอง 1 ช่องต่อ 1 รุ่นการแข่งขัน |
| 6 | **บัญชีคืนเงิน / รับเงินรางวัล** | บัญชีคืนเงินมัดจำ + บัญชีรับเงินรางวัล แต่ละส่วนมี checkbox "บันทึกข้อมูลเพื่อใช้ครั้งต่อไป" |
| 7 | **สรุปยอด** | คิดเงิน**แยกรายคัน**: ค่าสมัคร 7,000 + ค่ามัดจำ 10,000 = 17,000 ฿/คัน, ยอดรวม = ผลรวมทุกคัน |
| 8 | **ชำระเงิน** | บัตรเครดิต / QR-โอนเงิน + ออกใบกำกับภาษี + เงื่อนไขการชำระ |
| 9 | **สำเร็จ** | ใบเสร็จบนหน้าจอ + ปุ่มพิมพ์ **ใบเสร็จ PDF**; เข้าสู่ระบบให้อัตโนมัติแล้วพาไป "ประวัติของฉัน" |

### ขั้นตอนที่ 4 — ข้อมูลนักแข่ง (จุดเด่น)

- **หลายนักแข่ง**: ปุ่ม "เพิ่มนักแข่ง" เพิ่มการ์ดใหม่ต่อท้าย (ไม่เด้งขึ้นบน) — นักแข่งคนแรกลบไม่ได้, คนถัดไปลบได้
- แต่ละการ์ด **ย่อ/ขยาย (collapse/expand)** ได้
- **หลายรุ่นการแข่งขันต่อคน**: เลือกรุ่นจาก dropdown (มี 14 รุ่น) → เพิ่มได้หลายรุ่นด้วยปุ่ม "เพิ่มรุ่นแข่งขัน"
  - แต่ละรุ่นที่เลือกมี **ช่องกรอกเครื่องยนต์ (Model/Engine code)** และ **เบอร์รถแข่งของตัวเอง** แยกกัน
  - รุ่นที่เลือกแล้ว และเบอร์รถที่ใช้แล้วในคนเดียวกัน จะถูก disable กันเลือกซ้ำ

### ขั้นตอนที่ 5 — เลือกที่จอด (จุดเด่น)

- ต้องจองพีท **1 ช่องต่อ 1 รุ่นการแข่งขัน** (รวมทุกนักแข่งทุกรุ่น)
- แผงรายการช่องจองใต้ผัง: แถวที่ "กำลังเลือก" ไฮไลต์ → แตะพีทว่างบนผังเพื่อกำหนด แล้วเลื่อนไปแถวถัดไปอัตโนมัติ
- แตะพีทที่เลือกแล้วเพื่อยกเลิก, กันเลือกซ้ำ, ปุ่ม "ถัดไป" บล็อกจนเลือกครบ
- เต็นท์แดง = ถูกจอง (sponsor, คลิกดูข้อมูลผู้จองได้), เหลือง = TENT AIR, มีเลนขับรถ, ปุ่ม expand ดูผังเต็ม
- ตัวเลือก "จอดพีทถาวร (Private-Owned Pit)" เป็นทางเลือกแทนการเลือกจากผัง

---

## ประวัติของฉัน / My Profile

เข้าจากการเข้าสู่ระบบ หรือหลังลงทะเบียนสำเร็จ มี 3 แท็บ:

- **ข้อมูลของฉัน** — การ์ด **บัญชีผู้ใช้** (username + รหัสผ่านแบบซ่อน + ปุ่มเปลี่ยนรหัสผ่าน), ข้อมูลผู้สมัคร + รูปบัตร (แก้ไขได้)
- **ข้อมูลนักแข่ง** — การ์ดนักแข่ง (มีได้หลายคน) + ปุ่ม "เพิ่มนักแข่ง" (ข้ามไปขั้นตอน 4)
- **ประวัติการลงทะเบียน** — ตารางประวัติ (ต่อรุ่น/ต่อคัน)

---

## Dashboard ผู้ดูแล / Admin Dashboard

- เปิดด้วยคีย์ลัด **`Ctrl + Shift + Z`** → หน้าเข้าสู่ระบบแอดมิน → Dashboard
- **KPI**: จำนวนผู้ลงทะเบียน · จำนวนนักแข่ง · ยอดเงินที่ชำระแล้ว
- **ตาราง 2 แท็บ** (ผู้สมัคร / นักแข่ง) — 20 แถว/หน้า + paging + **Export CSV**
- แท็บนักแข่งมี **filter ค้นหา** (ชื่อ, รถ/เครื่องยนต์, รุ่นการแข่งขัน [dropdown], ทีม/อู่, เบอร์รถ)
- ดูรูปบัตร/รูปนักแข่งใน modal, แก้ไขข้อมูลแต่ละแถว (รวมลบ/อัปโหลดรูป), และดู **ใบสมัครนักแข่งเป็น PDF**

---

## กติกาการคิดเงิน / Pricing

- **1 รุ่นการแข่งขัน = 1 คัน = 1 พีท**
- ต่อคัน: ค่าสมัคร **7,000** + ค่ามัดจำการแข่งขัน **10,000** = **17,000 ฿**
- ยอดชำระทั้งหมด = 17,000 × จำนวนคัน (รวมทุกนักแข่งทุกรุ่น)

---

## โครงสร้างไฟล์ / Files

| ไฟล์ | หน้าที่ |
|------|--------|
| `index.html` | แอปหลัก (สำหรับ GitHub Pages) — เป็น source of truth |
| `souped-up.html` | เวอร์ชันสำหรับ Artifact — **สร้างจาก `index.html`** (ตัด document skeleton ออก) อย่าแก้มือ |
| `asset/` | สื่อต้นฉบับ: `logo-main.png`, `logo-main-b.png` (โลโก้เข้มบนใบเสร็จ), `img_racer.png` (รูปนักแข่ง), `idcard.png` (รูปบัตร), `bg-2.png`, `souped-bg_1.png`, `favicon-16x16.png` |
| `README.md` | สรุปย่อโปรเจกต์ (TH/EN) |
| `CLAUDE.md` | คู่มือสำหรับ Claude Code (สถาปัตยกรรม + workflow) |

> **หมายเหตุ:** ภาพในแอปฝังเป็น base64 ใน `index.html` — การเปลี่ยนไฟล์ใน `asset/` ต้อง **re-embed base64** เข้าโค้ดใหม่ ไฟล์ใน `asset/` เป็นต้นฉบับ ไม่ใช่สิ่งที่แอปโหลดตรงๆ

---

## Deploy

- พัฒนาบน branch `claude/souped-up-registration-system-bnxppt` และ mirror ไป `main`
- GitHub Pages เสิร์ฟจาก `main` (root) → https://nidss.github.io/su/
- ทุกครั้งที่แก้ `index.html` ต้อง regenerate `souped-up.html` แล้ว commit ทั้งคู่, push ทั้งสอง branch, และ republish Artifact
- โค้ดส่วนลดตัวอย่าง (ถ้ามีในบางเวอร์ชัน): `SOUPEDUP`, `EARLYBIRD`, `VIP2026`
- Export CSV / พิมพ์ PDF ใช้งานได้จริงบน GitHub Pages/ไฟล์ในเครื่อง (บนหน้า Artifact การดาวน์โหลดถูก sandbox บล็อก)

---

© 2026 NIDSS · สงวนลิขสิทธิ์
